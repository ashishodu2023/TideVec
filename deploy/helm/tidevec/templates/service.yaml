apiVersion: v1
kind: Service
metadata:
  name: {{ include "tidevec.fullname" . }}
  labels:
    {{- include "tidevec.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tidevec.selectorLabels" . | nindent 4 }}
