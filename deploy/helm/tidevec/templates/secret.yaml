apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tidevec.fullname" . }}-secret
  labels:
    {{- include "tidevec.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-key: {{ .Values.secrets.apiKey | default (randAlphaNum 32) | quote }}
